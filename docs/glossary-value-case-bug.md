# Баг: кривой регистр значений глоссария

Статус: **исправлено** (задокументировано 2026-07-04, исправлено 2026-07-09;
осталась платная ретрансляция — см. «Как чинилось» ниже)

## Как чинилось (2026-07-09)

Реализован вариант 1 + фикс `sourceText` из варианта 2:

- **Данные.** Все значения в `data/glossary.json5` приведены к «серединному»
  регистру: строчная буква у нарицательных, заглавная только у имён собственных
  (Инь, Ян, Дао, Будда, Даньтянь, Тайву, секты/города/регионы, Курган мечей).
  Отступление от списка в этом доке: `Gu → гу` со строчной — по прецеденту
  самих данных («Личинка гу», «гу чёрной крови»), это нарицательное.
- **Код.** `matchGlossary` теперь сохраняет словоформу термина из текста
  (новое поле `GlossaryMatch.src`, регистр первого вхождения), и
  `glossaryPairsForTexts` отдаёт её как `sourceText` вместо лауеркейза.
  В сигнатуру глоссария (ключи кэша/`srcHash`) `src` НЕ входит.
- **Миграция ручных правок.** Смена значений инвалидировала кэш-ключи текстов
  с терминами. Из пяти ручных коммитов кэша (`c981eb4`, `06a7b8e`, `acf6577`,
  `ff5b24b`, `26cb5f0`) только одна живая запись содержала термин — она
  перенесена на новый ключ (и в её значении заодно поправлен регистр:
  «Расходуется энергия…»). Остальные 29 ключей не изменились.
- **TM.** `npm run rebuild-tm` (cache-only) подхватил миграцию; старые машинные
  переводы остались в TM как stale и продолжают попадать в оверлей.

## Продолжение: точечный case-sensitive матчинг (`cs`, 2026-07-09)

Раз регистр значений теперь значим, ключам тоже дали регистр — опт-ин:
значение-объект получило флаг `cs: true` (термин матчится с учётом регистра;
ключ обязан содержать заглавную). Зачем: у части терминов строчная форма — это
обычная проза с другим смыслом, и глоссарий портил перевод: `attack` (глагол,
1269 строчных вхождений в корпусе), `force` («sheer force» ≠ «мощь»), `sway`
(ивы качаются, а не «смятение»), `insight`, `stab`/`hack`/`upswing` (значения —
прилагательные типов атак). Эти 7 терминов помечены `cs`. Глобальный
case-sensitive НЕ вариант: `sect`/`qi`/`poison`/`martial arts` в строчной
форме — те же понятия (тысячи вхождений), им глоссарий нужен.

Механика: лоадер держит cs-ключи в карте дословно (не лауеркейзит), матчер
компилирует два регекса (ci + cs) и сливает кандидатов «левее-длиннее», так что
ci-«Attack Speed» по-прежнему съедает вложенный cs-«Attack» и сигнатуры
ci-текстов не меняются. CSV-раунд-трип получил колонку `cs`. Добавочная
инвалидация кэша — ноль (тексты с этими терминами уже были инвалидированы
рекейсом), ~200+231 текстов даже вернулись к бесплатным записям кэша.

**Осталось:** платная ретрансляция инвалидированных юнитов — 63 660 текстов
кэша / ~81.7k stale-юнитов TM, ~11.9M символов ≈ **5 000 RUB** по тарифу
419 RUB/1M. Запуск: `npm run translate-all`, затем `npm run rebuild-tm` не
нужен (переводы пишутся сразу), затем `npm run apply-all`. Старые кэш-записи
с прежними сигнатурами оставлены в файле намеренно — дешёвый откат, если
значения глоссария вернуть.

## Симптом

Термины глоссария в переводе получают неправильный регистр: нарицательные
существительные выходят с заглавной буквы посреди русского предложения.

> «восстанови свою **Ци** в **Секте**» ← должно быть «...ци... в секте»

При этом настоящие имена собственные (Инь, Ян, Дао, Будда, Даньтянь) выглядят
правильно — поэтому поведение кажется «кривым»: часть терминов капитализируется
верно, часть нет.

## Корневая причина

Все **221 из 221** RU-значений в [`data/glossary.json5`](../data/glossary.json5)
записаны **с заглавной буквы**, включая нарицательные:

```
Qi           → Ци
Martial Art  → Боевая техника
Deviation    → Отклонение
Inner Breath → Внутреннее дыхание
```

Значений со строчной буквы — ноль. Мид-предложения такие термины должны быть со
строчной; заглавная нужна только именам собственным.

В [`src/engine/yandex.ts`](../src/engine/yandex.ts) (`glossaryPairsForTexts`,
~строка 165) значение уходит в пару **дословно** (`translatedText: ru`) с
`exact: false`. Флаг `exact:false` заставляет нейроглоссарий склонять термин по
грамматике, но **ведущую заглавную букву тащит в вывод**. Ни `pipeline.ts`, ни
матчер регистр значения нигде не нормализуют.

Проверка данных:

```bash
node -e '
const JSON5=require("json5");const fs=require("fs");
const p=JSON5.parse(fs.readFileSync("data/glossary.json5","utf8"));
let cap=0,low=0;
for(const [k,v] of Object.entries(p)){
  if(k.startsWith("_"))continue;
  const ru=typeof v==="string"?v:v.ru; if(!ru)continue;
  const f=ru[0];
  if(f!==f.toLowerCase())cap++; else if(f!==f.toUpperCase())low++;
}
console.log("UPPERCASE-initial:",cap,"lowercase-initial:",low);
'
# => UPPERCASE-initial: 221 lowercase-initial: 0
```

## Побочный, более мелкий момент

В [`src/glossary/match.ts`](../src/glossary/match.ts) (~строка 65) исходный
термин лауеркейзится (`m[0].toLowerCase()`), и этот же строчный вид становится
`sourceText` пары ([`yandex.ts`](../src/engine/yandex.ts) ~строка 176), тогда как
в тексте запроса регистр сохраняется. Пример:

```
REQUEST TEXT:  "True Qi restored. Your Qi grows in the Sect."
GLOSSARY PAIR: {"sourceText":"true qi", ...}   // строчные против "True Qi" в тексте
```

Латентно: сработает как баг, только если матчинг Яндекса чувствителен к регистру
букв (документация про это молчит; feed-механизм для точек намекает, что матчинг
довольно буквальный). Не корневая причина, но стоит поправить заодно.

## Варианты фикса

1. **Данные (основной).** Привести значения к «серединному» регистру: строчная
   буква для нарицательных, заглавная только для имён собственных
   (Инь, Ян, Дао/Тао, Будда, Даньтянь, Гу, названия сект/мест и т.п.).
   - Плюс: бьёт прямо в причину, прозрачно.
   - Минус: термин в начале предложения может выйти со строчной (Яндекс, правда,
     часто сам капитализирует начало предложения).
   - После правки нужен рефреш TM (значения меняются → сигнатура глоссария
     инвалидирует затронутые юниты; глобальный `GLOSSARY_VERSION` бампать не надо,
     хватит per-text srcHash).

2. **Код (контекстный регистр).** В `glossaryPairsForTexts` приводить ведущую
   букву значения к регистру найденной словоформы в тексте (заодно чинит и
   `sourceText` из побочного пункта — для этого надо сохранить оригинальный
   `m[0]` в матче, а не лауеркейзить).
   - Ограничение: Яндекс применяет **один глоссарий на батч**, поэтому «в начале
     предложения — заглавная, в середине — строчная» для одного и того же термина
     в одном батче не выразить. Придётся выбирать одну форму на пару.

Рекомендация: **вариант 1** как основной, при желании + фикс `sourceText`.

## Затронутые файлы

- [`data/glossary.json5`](../data/glossary.json5) — источник (все значения капитализированы)
- [`src/engine/yandex.ts`](../src/engine/yandex.ts) — `glossaryPairsForTexts`, дословный `translatedText`, лауеркейз `sourceText`
- [`src/glossary/match.ts`](../src/glossary/match.ts) — `matchGlossary` лауеркейзит `m[0]`
- [`src/engine/lmstudio.ts`](../src/engine/lmstudio.ts) — LM-путь тоже отдаёт значение дословно в промпт (LLM к регистру устойчивее, но термин тот же)
