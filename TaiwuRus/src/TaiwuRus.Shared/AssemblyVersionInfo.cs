using System.Reflection;
using TaiwuRus.Shared;

// GenerateAssemblyInfo is off (Directory.Build.props), so the assembly version is declared
// here, single-sourced from ModInfo.Version, and linked into every project.
[assembly: AssemblyVersion(ModInfo.Version)]
[assembly: AssemblyFileVersion(ModInfo.Version)]
